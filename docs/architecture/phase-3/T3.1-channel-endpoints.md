# T3.1 — Tabla `channel_endpoints`

**Estado:** Pendiente  
**Tipo:** DB migration  
**Archivo:** `migrations/002_channel_endpoints.sql`  
**Riesgo:** Ninguno — tabla nueva, nada la usa aún

---

## Objetivo

Crear la tabla que es el corazón del routing multi-tenant: el mapa entre cada número de Twilio y la empresa que lo posee.

---

## Por qué esta tabla existe

En FASE 2, el servidor sabía a qué empresa pertenecía porque `COMPANY_SLUG` estaba hardcodeado en las variables de entorno. Solo podía haber una empresa por servidor.

Para multi-tenant necesitamos que el servidor pregunte: "este mensaje llegó al número `whatsapp:+521XXXXXXXXXX`, ¿quién es el dueño?" Esa pregunta requiere una tabla de respuestas.

El nombre `channel_endpoints` (no `phone_numbers`) es una decisión deliberada de la Constitución: cuando TARA soporte Instagram DM, email o Telegram, los endpoints de esos canales también vivirán en esta tabla. La abstracción es correcta desde el inicio.

---

## Schema

```sql
CREATE TABLE channel_endpoints (
  id          uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  company_id  uuid NOT NULL REFERENCES companies(id),
  endpoint    text NOT NULL,    -- formato completo: "whatsapp:+521XXXXXXXXXX"
  canal       text NOT NULL DEFAULT 'whatsapp',
  activo      boolean NOT NULL DEFAULT true,
  created_at  timestamptz DEFAULT now(),

  UNIQUE (endpoint)
);
```

**Nota sobre `workspace_id`:** La Constitución define Workspace como nivel por debajo de Company. En FASE 3, los workspaces aún viven dentro de la tabla `companies`/`personalities`. El campo `workspace_id` se agregará en FASE 4 cuando la tabla `workspaces` exista como entidad propia. No se agrega ahora para no crear una columna nullable que no se puede poblar.

**Índice primario:**
```sql
CREATE INDEX idx_channel_endpoints_lookup
  ON channel_endpoints (endpoint, activo);
```

Este es el índice que se usa en cada mensaje entrante. Debe ser rápido.

**Sin RLS:** misma razón que `decision_logs`. La anon key vive en el servidor (Render env vars), nunca en el navegador. El acceso a esta tabla es exclusivamente server-side.

---

## Verificación post-ejecución

```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'channel_endpoints'
ORDER BY ordinal_position;
```

Resultado esperado: 6 columnas (id, company_id, endpoint, canal, activo, created_at).

```sql
SELECT indexname FROM pg_indexes
WHERE tablename = 'channel_endpoints';
```

Resultado esperado: `channel_endpoints_pkey` + `idx_channel_endpoints_lookup`.

---

## Definition of Done

- [ ] SQL ejecutado en Supabase SQL Editor sin errores
- [ ] Verificación de 6 columnas ejecutada y confirmada
- [ ] Verificación de índices ejecutada y confirmada
- [ ] RLS deshabilitado (o no activado — `DISABLE ROW LEVEL SECURITY` ejecutado por precaución)
- [ ] Archivo `migrations/002_channel_endpoints.sql` commiteado al repo
- [ ] Commit realizado: `"feat(db): migration 002 — tabla channel_endpoints"`

---

## Rollback

```sql
DROP TABLE IF EXISTS channel_endpoints;
```

Sin consecuencias porque ningún código la usa aún.
